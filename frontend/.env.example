# Browser-visible backend base URL when needed.
# The app also proxies /api/backend/* through Next.js.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional server-side backend URL used by API routes/proxy integrations.
BACKEND_API_URL=http://localhost:8000
